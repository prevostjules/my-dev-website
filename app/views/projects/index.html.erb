<% content_for :meta_title, "Tous les projets de #{DEFAULT_META["meta_product_name"]} - Développeur web" %>

<div class="div-banana p-3">
  <div class="container">
    <h2>Mes projets</h2>
    <div class="card-container">
      <% @projects.each do |project| %>
        <%= render "shared/card-project", project: project %>
      <% end %>
    </div>
  </div>
</div>
