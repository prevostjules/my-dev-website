<div class="card">
  <%= cl_image_tag project.photo.key, alt: "test", secure: true, class: "card-img" %>
  <div class="card-infos">
    <h1><%= project.title %></h1>
    <p>CreeksApp permet aux coachs / influenceurs de réaliser des streams à une audience payante et aux fans de visionner du contenu exclusif.</p>
    <div class="card-btns">
      <%= link_to "Plus d'infos", project_path(project), class: "btn btn-primary" %>
    <%= link_to "Voir le site", project.link, target: "_blank", rel: "nofollow", class: "btn btn-primary" %>
    </div>
    <hr>
    <p class="card-time mb-2">4 personnes ont réalisé ce projet en 9 jours.</p>
    <div class="card-tags">
      <% project.tags.each do |tag| %>
        <span class="badge badge-<%= random_class %>"><%= tag.name %></span>
      <% end %>
    </div>
  </div>

</div>
